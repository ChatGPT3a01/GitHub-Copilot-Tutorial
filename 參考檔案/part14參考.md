Part 14 — 第 1 張截圖
📸 14-1.png：GET 請求（尚未送出）
你現在只要做這一張
操作步驟（逐步照做）：

開啟 VS Code

開啟 Copilot Chat

快捷鍵：Ctrl + Alt + I

在 Copilot Chat 輸入框 中輸入 以下這一句（完整照打）：

請用 Python requests 寫一個 GET 請求的函式，可以取得指定 URL 的 JSON 資料


停在這裡，不要按 Enter

直接截圖

截圖畫面必須包含：

Copilot Chat 面板

Chat 輸入框中顯示上面那段文字

尚未送出的狀態（沒有回應）

儲存檔案：
D:\GitHub Copilot教學\images\part14\14-1.png


📣 做完請回我一句：
👉 14-1 截圖完成

我才會進到 14-2。

14-1 截圖完成

帥氣的亮：
好 👍 14-1 已完成，往下一張走。

▶️ Part 14 — 第 2 張截圖
📸 14-2.png：GET 請求結果（Copilot 回應）
你現在只做這一張
操作步驟（一步一步）：

延續剛剛的 Copilot Chat 畫面

在剛剛已輸入的那句 GET 請求提問上
👉 按下 Enter 送出

等待 Copilot 完整回應

會看到一段 使用 requests 的 Python GET 函式

不要插入程式碼、不用修改

直接截圖

截圖畫面必須包含：

Copilot Chat 面板

Copilot 產生的 GET 請求程式碼

可清楚看到 requests.get(...)、response.json() 之類內容

儲存檔案：
D:\GitHub Copilot教學\images\part14\14-2.png


📣 完成後請回覆我一句：
👉 14-2 截圖完成

我才會繼續 14-3（POST 請求）。

14-2 截圖完成

帥氣的亮：
好 👍 14-2 已完成，繼續下一張。

▶️ Part 14 — 第 3 張截圖
📸 14-3.png：POST 請求（Copilot 產生程式碼）
現在只做這一張，不要多做
操作步驟（照做即可）：

保持 Copilot Chat 開啟

在 Chat 輸入框中，輸入以下文字（原樣即可）：

請用 Python requests 寫一個 POST 請求的函式，可以發送 JSON 資料到指定 URL


按 Enter 送出

等待 Copilot 回應完整的 POST 請求程式碼

不用插入、不用修改，只要看到程式碼即可

截圖畫面必須包含：

你的 提問內容

Copilot 產生的 POST 請求程式碼

程式碼中可看到：

requests.post(...)

json=... 或 data=...

儲存檔案：
D:\GitHub Copilot教學\images\part14\14-3.png


📣 完成後請回覆我：
👉 14-3 截圖完成

我才會帶你進行 14-4：處理 JSON 回應。

14-3 截圖完成

帥氣的亮：
收到 ✅ 14-3 截圖完成

▶️ Part 14 — 第 4 張截圖
📸 14-4.png：處理 JSON 回應

現在只做這一張，其它都先不要。

操作步驟（完全照你的資料）：

保持 Copilot Chat 開啟

在 Chat 輸入框中，輸入以下文字（原樣即可）：

請示範如何解析 API 回傳的 JSON 資料，並取出特定欄位


按 Enter 送出

等待 Copilot 回應

截圖時機：

Copilot 回應完成

畫面中清楚看到 JSON 解析的程式碼

截圖畫面必須包含：

你的提問內容

Copilot 產生的程式碼

程式碼中可看到：

response.json()

取出欄位（例如 data["key"] 或類似）

儲存路徑：
D:\GitHub Copilot教學\images\part14\14-4.png


📣 完成後請回覆我：
👉 14-4 截圖完成

我才會帶你進到 14-5：錯誤處理。

14-4 截圖完成

帥氣的亮：
收到 ✅ 14-4 截圖完成

▶️ Part 14 — 第 5 張截圖
📸 14-5.png：錯誤處理（API 請求失敗處理）

現在只做這一張，不進行下一張。

操作步驟（完全依你給的資料）：

保持 Copilot Chat 開啟

在 Chat 輸入框中，輸入以下文字（原樣即可）：

請為這個 API 請求加上錯誤處理，包含網路錯誤和回應狀態碼檢查


按 Enter 送出

等待 Copilot 回應

截圖時機：

Copilot 回應完成

顯示包含錯誤處理的程式碼

截圖畫面必須包含：

你的提問內容

Copilot 產生的程式碼

程式碼中可看到：

try / except

狀態碼檢查（如 response.status_code 或 response.raise_for_status()）

儲存路徑：
D:\GitHub Copilot教學\images\part14\14-5.png


📣 完成後請回覆我：
👉 14-5 截圖完成

我才會帶你進行 14-6：實際 API 範例。

14-5 截圖完成

帥氣的亮：
很好 👍 14-5 已完成，繼續下一張。

▶️ Part 14 — 第 6 張截圖
📸 14-6.png：實際 API 範例（串接真實 API）

現在只做這一張，其它都先不要。

操作步驟（完全依你給的資料，不延伸）：

保持 Copilot Chat 開啟

在 Chat 輸入框中，輸入下面其中一種（擇一即可，照打）：

範例 A（天氣 API）：

請用 Python 寫一個函式，串接免費的天氣 API，取得資料並顯示


或 範例 B（匯率 API）：

請用 Python 寫一個函式，串接免費的匯率 API，取得資料並顯示


按 Enter 送出

等待 Copilot 回應完整程式碼

截圖時機：

Copilot 回應完成

顯示「實際 API 串接」的完整程式碼

截圖畫面必須包含：

你的提問內容

Copilot 產生的 API 串接程式碼

程式碼中可看到：

API URL

requests.get(...)

JSON 解析（例如 response.json()）

儲存路徑：
D:\GitHub Copilot教學\images\part14\14-6.png


📣 完成後請回覆我一句：
👉 14-6 截圖完成

接下來我會等你，再進 Part 15。

14-6 截圖完成

帥氣的亮：
👏 Part 14 全部完成！做得非常扎實。

✅ Part 14：實戰應用 - API 整合（完成確認）

你已依序完成以下 6 張截圖，且流程完全正確：

✔️ 14-1：GET 請求提問

✔️ 14-2：GET 請求程式碼結果

✔️ 14-3：POST 請求程式碼

✔️ 14-4：JSON 回應解析

✔️ 14-5：API 錯誤處理（try / except + 狀態碼）

✔️ 14-6：實際公開 API 串接範例

這一整個 Part 14 的教學結構，已經是可以直接拿去對老師或學生教的等級，而且截圖順序、畫面邏輯都很乾